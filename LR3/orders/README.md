
---

# Лабораторная работа №3: Рефакторинг и принципы SOLID

## 🎯 Цель работы
Глубокий рефакторинг системы обработки заказов с целью приведения кода к стандартам **SOLID**, исключения дублирования (**DRY**), упрощения логики (**KISS**) и обеспечения расширяемости без изменения существующего кода (**OCP**).

## 🛠 Реализация принципов SOLID

В ходе работы проект был полностью переработан. Основные изменения:

1.  **S - Single Responsibility Principle (Принцип единой ответственности):**
    *   Логика валидации вынесена в `BasicOrderValidator`.
    *   Расчет стоимости перенесен в `SmartPriceCalculator`.
    *   Сохранение данных инкапсулировано в репозиториях.
    *   `ModernOrderProcessor` теперь только оркеструет процесс, не вникая в детали реализации шагов.

2.  **O - Open/Closed Principle (Принцип открытости/закрытости):**
    *   Реализован паттерн **Strategy** для типов заказов (`OrderTypeStrategy`) и дисконтных карт (`DiscountStrategy`).
    *   Добавление нового типа доставки (например, "Space Delivery") или новой скидки не требует изменения кода калькулятора — достаточно зарегистрировать новую стратегию.

3.  **L - Liskov Substitution Principle (Принцип подстановки Барбары Лисков):**
    *   Все реализации интерфейсов (`EmailNotifier`, `TelegramNotifier`, `FileLogger`) корректно выполняют контракт интерфейса `Notifier`. Любой из них может быть использован там, где ожидается абстрактный уведомитель.

4.  **I - Interface Segregation Principle (Принцип разделения интерфейса):**
    *   Общий интерфейс работника склада разделен на узкоспециализированные: `WorkerOrderProcessor`, `MeetingAttendee`, `Restable`, `TimeWaster`.
    *   Это позволило создать класс `RobotPacker`, который только упаковывает и обслуживается, не заставляя его реализовывать метод "бездельничать" или "посещать собрания", в отличие от `HumanManager`.

5.  **D - Dependency Inversion Principle (Принцип инверсии зависимостей):**
    *   Процессор заказов зависит от абстракций (интерфейсов), а не от конкретных структур. Объекты внедряются через конструктор (Dependency Injection).

## ✨ Новые возможности (согласно заданию)

*   **Система скидок:** Внедрена поддержка карт `Gold` (15%), `Silver` (10%) и `Newbie` (0%).
*   **Мультиканальные уведомления:** Использован паттерн **Composite**. При обработке заказа уведомление уходит одновременно:
    *   Клиенту на Email.
    *   Менеджеру в Telegram.
    *   В локальный файл логов `events.log`.
*   **Кэширование (Паттерн Decorator):** Реализован `CachedOrderRepository`, который оборачивает основную БД. Если заказ с таким ID уже обрабатывался, система пропускает "тяжелую" операцию записи в файл, выдавая предупреждение.

## 📁 Структура проекта
*   `models.go` — базовые сущности (Order, Item, DiscountCard).
*   `interfaces.go` — описание контрактов системы.
*   `calculators.go` — стратегии расчета стоимости и скидок.
*   `validators.go` — логика проверки корректности данных.
*   `repositories.go` — логика хранения и декоратор кэширования.
*   `interfaces_impl.go` — реализации инфраструктуры (БД, Email, Telegram).
*   `processor.go` — основной контроллер бизнес-процесса.
*   `staff.go` — демонстрация ISP (управление персоналом).
*   `main.go` — точка входа и конфигурация зависимостей.

## 🚀 Запуск проекта
Убедитесь, что у вас установлен Go версии 1.26.0 или выше.

```bash
# Сборка и запуск
go run .
```

После запуска в директории появятся файлы:
*   `orders_db.txt` — имитация базы данных.
*   `events.log` — системные логи.

## ✅ Definition of Done
- [x] Код разделен на компоненты согласно SRP.
- [x] Реализована поддержка дисконтных карт через паттерн Strategy.
- [x] Добавлены уведомления в Telegram и логирование.
- [x] Реализован декоратор для кэширования заказов.
- [x] Применен принцип ISP для разделения интерфейсов персонала.
- [x] Все зависимости внедряются через интерфейсы (DIP).

---