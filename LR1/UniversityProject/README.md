
---

# Лабораторная работа №1: Основы ООП и Инкапсуляция

## 🎯 Цель работы
Разработать абстрактную модель предметной области **"Университет"**, используя базовые концепции ООП, с особым акцентом на **инкапсуляцию** и защиту целостности данных.

## 🏛️ Описание предметной области
Система представляет собой иерархическую структуру университета, включающую:
*   **Организационную структуру:** Университет → Факультеты → Кафедры.
*   **Учебный процесс:** Курсы, расписание, аудиторный фонд.
*   **Участников:** Студенты и преподаватели (на базе общего класса `Person`).
*   **Учет успеваемости:** Записи о регистрации на курсы (`Enrollment`) и оценки (`Grade`).

## 🛠️ Реализация инкапсуляции
В данной работе принцип инкапсуляции реализован через:
1.  **Закрытие полей:** Все поля классов имеют модификатор доступа `private`.
2.  **Валидацию данных:** Входные параметры проверяются в конструкторах и сеттерах (например, проверка на пустые строки, корректность Email, диапазон оценок от 0 до 100, положительная вместимость аудиторий).
3.  **Методы доступа:** Доступ к полям осуществляется через публичные `get` и `set` методы.
4.  **Защиту внутренних коллекций:** Методы возвращают копии списков (например, `getEnrollments()` в классе `Student`), чтобы предотвратить несанкционированное изменение внутренних данных объекта извне.

## 📊 Список реализованных классов (11 классов)
1.  `University` — главный контейнер системы.
2.  `Faculty` — факультет университета.
3.  `Department` — кафедра внутри факультета.
4.  `Person` — базовый класс для людей.
5.  `Teacher` — преподаватель (наследует `Person`).
6.  `Student` — студент (наследует `Person`).
7.  `Course` — учебная дисциплина.
8.  `Classroom` — информация об аудитории.
9.  `Schedule` — связь курса, времени и места.
10. `Enrollment` — связь студента с курсом.
11. `Grade` — оценка студента за курс.

## 🚀 Демонстрационный сценарий(Lab 1)
В классе `demo.UniversityDemoLab1` реализован комплексный сценарий:
1.  Инициализация структуры БГУИР.
2.  Создание преподавательского состава и назначение заведующих кафедрами.
3.  Распределение аудиторного фонда.
4.  Создание курсов и формирование расписания.
5.  Регистрация студентов на курсы.
6.  Симуляция процесса выставления оценок и расчет среднего балла (GPA).

**Как запустить:**
```bash
# Из корневой директории проекта
javac -d bin src/models/*.java src/demo/*.java
java -cp bin demo.UniversityDemoLab1
```

## 📈 UML-диаграмма
Диаграмма классов, отображающая отношения между созданными сущностями, находится в директории:
`./diagram/class_diagram.png`

## ✅ Definition of Done (Проверка выполнения)
- [x] Разработана UML-схема классов.
- [x] Предметная область (Университет) описана полно.
- [x] Создано более 10 классов (итого 11 + демо-класс).
- [x] Реализована инкапсуляция (private fields, валидация).
- [x] Создан демонстрационный сценарий взаимодействия.

---