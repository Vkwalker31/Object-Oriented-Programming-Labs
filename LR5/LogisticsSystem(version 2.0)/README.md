
---

# Лабораторная работа №5: GRASP и паттерны GoF (порождающие, структурные, поведенческие)

## 🎯 Цель работы
Проектирование логистической системы для расчета стоимости и скорости доставки грузов. Основное внимание уделено распределению обязанностей между классами согласно принципам **GRASP** и реализации гибкого создания объектов через **порождающие паттерны GoF**. В LR5 добавлен модуль работы с внешними данными и применены **структурные** и **поведенческие** паттерны.

## 🏛️ Реализация принципов GRASP

1.  **Information Expert (Информационный эксперт):**
    *   Метод расчета стоимости части груза находится в классе `CargoItem`, так как только он обладает знаниями о своей массе ($$m_i$$) и тарифе ($$c_i$$).
    *   Итоговый расчет стоимости доставки ($$P$$) делегирован классу `LogisticsService`, который собирает информацию от всех участников процесса.
2.  **Creator (Создатель):**
    *   Создание объектов транспорта делегировано конкретным фабрикам (напр. `AirLogisticsFactory`), так как они обладают всей необходимой информацией о характеристиках судов, самолетов и машин.
3.  **Controller (Контроллер):**
    *   Класс `LogisticsController` (или `Main`) принимает ввод от оператора, преобразует его в системные команды и управляет процессом расчета, не выполняя саму математику.
4.  **Low Coupling (Низкая связанность):**
    *   Бизнес-логика не зависит от конкретных реализаций транспорта благодаря использованию интерфейса `Transport`.

## 🏗️ Порождающие паттерны (GoF)

В работе применен паттерн **Abstract Factory (Абстрактная фабрика)**:
*   Интерфейс `LogisticsFactory` определяет методы для создания транспорта.
*   Конкретные фабрики (`AirFactory`, `GroundFactory`, `SeaFactory`) инкапсулируют логику создания специфических объектов. Это позволяет добавлять новые виды транспорта (например, "Космический"), не меняя существующий код (соблюдение **OCP** из SOLID).

## 🔢 Математическая модель
Расчет стоимости доставки осуществляется по формуле:

$$P = \sum (c_i \cdot m_i) + r \cdot p_{delivery}$$

Где:
*   $$c_i$$ — стоимость перевозки 1 кг груза вида $$i$$;
*   $$m_i$$ — общая масса груза вида $$i$$ в партии;
*   $$r$$ — расстояние до точки назначения;
*   $$p_{delivery}$$ — накладные расходы вида транспорта на одну единицу расстояния.

## 📦 Модуль данных (LR5): структурные и поведенческие паттерны

- **Входные данные:** файлы JSON, XML, CSV с перечнем грузов, расстоянием и опционально типом транспорта (пакет `com.logistics.io.adapter` — **Adapter**).
- **Фасад:** `DataModuleFacade` — единая точка входа: загрузка файла, расчёт вариантов доставки. Если тип транспорта не задан, возвращается список вариантов по всем видам транспорта (**Facade**).
- **Экспорт:** сохранение в JSON или CSV; декораторы добавляют шифрование и/или сжатие в .zip, с возможностью комбинирования (**Decorator**, **Strategy**).
- **Фильтрация и сортировка:** по полям (название транспорта, цена, скорость); сортировки комбинируются (**Strategy**).
- **Итератор:** обход вариантов доставки без раскрытия внутренней структуры (`DeliveryOptionsResult`, **Iterator**).

### Форматы входных файлов
- **JSON:** `{"cargo":[{"type":"electronics","quantity":5}],"distance":500,"transportType":"","destination":"Minsk"}`
- **CSV:** первая строка — `distance,transportType,destination`; далее строки — `type,quantity` для каждого груза.
- **XML:** корневой элемент с дочерними `cargo` (type, quantity), `distance`, `transportType`, `destination`.

## 🚀 Инструкция по запуску

Для компиляции и запуска проекта убедитесь, что у вас установлены **JDK 17** и **Maven**.

### Сборка и запуск (Maven):
```bash
mvn compile exec:java
```
Тесты: `mvn test`

### Альтернатива (терминал):
1. Перейдите в корневую директорию проекта.
2. Скомпилируйте исходный код в папку `out`:
```bash
javac -d out src/com/logistics/**/*.java
```
3. Запустите: `java -cp out com.logistics.Main`

## 📊 Пример сценария
1. Оператор вводит параметры груза (напр. "Электроника", 100кг).
2. Оператор выбирает дистанцию (напр. 500 км).
3. Система предлагает выбрать тип транспорта: `Air`, `Ground`, `Sea`.
4. Система выводит детализированный расчет стоимости и примерное время в пути.

---