Вот проект файла **README.md** для твоей второй лабораторной работы, составленный по аналогии с первой, но с учетом новых требований к наследованию, композиции и полиморфизму.

---

# Лабораторная работа №2: Наследование, композиция и полиморфизм

### 🎯 Цель работы
Освоить способы имплементации сложных связей между объектами и расширить возможности переиспользования кода, используя базовые принципы ООП: наследование (отношение **is-a**), объектную композицию (отношение **has-a**) и различные виды полиморфизма.

### 🏛️ Расширение предметной области
В рамках работы абстрактная модель университета была детализирована и разделена на уровни абстракции:
1.  **Иерархия людей:** Выделен базовый класс `Person`, от которого наследуются `Student` и `Teacher`.
2.  **Иерархия курсов:** Базовый логический уровень вынесен в абстрактный класс `AbstractCourse`, который разделяется на конкретные реализации: `LectureCourse` (лекции) и `LabCourse` (лабораторные работы).
3.  **Интерфейсы:** Введен интерфейс `Attendable`, описывающий контракт для сущностей, имеющих вместимость и систему учета присутствующих.

### 🛠️ Реализация ключевых концепций

#### 1. Наследование (Relation: Is-a)
Реализовано через создание иерархий классов:
*   `Student` и `Teacher` являются (`is-a`) типом `Person`.
*   `LabCourse` и `LectureCourse` являются (`is-a`) типом `AbstractCourse`.
*   Использование модификатора `protected` в `AbstractCourse` позволяет дочерним классам напрямую работать с полями родителя, сохраняя их скрытыми для внешних классов.

#### 2. Объектная композиция (Relation: Has-a)
Сложные связи между сущностями построены на включении одних объектов в другие:
*   **Композиция:** `University` содержит список `Faculty`. `Faculty` содержит `Department`. (Жизненный цикл частей жестко связан с целым).
*   **Агрегация:** `AbstractCourse` содержит объект `Schedule`, который в свою очередь ссылается на `Classroom`.

#### 3. Полиморфизм
В работе применены три основных вида полиморфизма:
*   **Полиморфизм подтипов (Subtype):** Использование ссылки типа `AbstractCourse` для работы с объектами `LabCourse` и `LectureCourse`. Вызов переопределенного метода `getCourseType()` разрешается в рантайме (позднее связывание).
*   **Параметрический полиморфизм:** Использование дженериков в стандартных коллекциях Java (например, `List<Enrollment>`), что позволяет методам работать с различными типами данных без дублирования кода.
*   **Ad hoc полиморфизм:** Реализован через перегрузку конструкторов и методов внутри классов для обработки различных наборов входных параметров.

### 📊 Список обновленных и новых сущностей
*   `Attendable` (Interface) — контракт на владение "вместимостью".
*   `Person` (Abstract Class) — базовые данные человека.
*   `AbstractCourse` (Abstract Class) — общая логика учебной дисциплины.
*   `LectureCourse` — расширение курса спецификой лекционных залов.
*   `LabCourse` — расширение курса требованиями к компьютерам и ПО.
*   `Schedule` — композиционный узел, связывающий курс, время и аудиторию.

### 🚀 Демонстрационный сценарий (Lab 2)
Класс `demo.UniversityDemoLab2` демонстрирует:
1.  Создание сложной структуры вуза (Факультеты -> Кафедры -> Преподаватели).
2.  **Полиморфную обработку:** создание единого списка `List<AbstractCourse>`, содержащего разные типы курсов, и итерация по ним с вызовом специфичного поведения.
3.  Связывание курсов с аудиториями через объект `Schedule`.
4.  Процесс записи студентов на разные типы курсов и автоматический пересчет `GPA`.

### 📦 Запуск работы
```bash
# Компиляция всех моделей и демо-класса
javac -d bin src/models/*.java src/demo/UniversityDemoLab2.java

# Запуск демонстрации ЛР №2
java -cp bin demo.UniversityDemoLab2
```

### 📈 UML-диаграмма
Обновленная диаграмма классов (`class_diagram_2.0.png`) отображает:
*   Пунктирные стрелки реализации интерфейсов (`implements`).
*   Сплошные стрелки наследования (`extends`).
*   Связи ромбовидной ассоциации для отображения композиции и агрегации.

---